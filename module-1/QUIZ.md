# Module 1 — Quiz

1. What does `StateGraph` provide that a simple chain does not?
2. How does a router node decide the next node?
3. Why separate state updates into dedicated nodes?
4. What is an agent loop and when is it useful?
5. How does memory change an agent’s behavior over multiple turns?
6. What would you include in a minimal state schema for a router?
