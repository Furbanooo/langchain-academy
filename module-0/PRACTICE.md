# Module 0 — Practice

1. Run the example in [module-0/basics.ipynb](../module-0/basics.ipynb) and write down the execution order of nodes.
2. Add a new node that appends a field to the state, then confirm the updated state in the output.
3. Modify the initial state values and observe how downstream nodes change their outputs.
4. Add a simple conditional branch (two possible next nodes) and test both paths.
5. Summarize the final state in a single sentence after execution.
